{
	"headings": {
		"aboutApiSettings": "Claude setup token settings"
	},
	"fields": {
		"config": {
			"enabled": {
				"title": "Enabled"
			},
			"accessToken": {
				"title": "Setup token",
				"placeholder": "Paste your setup token here",
				"description": "Run `claude setup-token` in your terminal and paste the generated token here."
			},
			"model": {
				"title": "Model",
				"placeholder": "e.g., claude-sonnet-4-20250514",
				"description": "The Claude model to use for generating responses"
			}
		}
	},
	"messages": {
		"config": {
			"edited": "Claude setup token settings have been updated",
			"notEdited": "Failed to update Claude setup token settings"
		}
	},
	"texts": {
		"aboutApiSettings": "Configure your Anthropic Claude connection using a setup token. Run `claude setup-token` in your terminal to get a token from your Claude subscription."
	}
}
