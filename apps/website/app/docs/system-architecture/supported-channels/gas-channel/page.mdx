import { Callout } from 'nextra/components'

# Gas Channel

<Callout type="info">
	**Channel Category**: `gas`
</Callout>

Detects combustible or toxic gas presence and related alarm states for safety monitoring.

---

## Required Properties

| **Property** | **Data Type** | **Range/Values** | **Unit** | **Permissions** |
|--------------|---------------|------------------|----------|-----------------|
| `detected` | Bool | - | - | RO (Read-Only) |
| `status` | Enum | `normal`, `warning`, `alarm` | - | RO (Read-Only) |

---

### Property Details

#### `detected`

Binary detection state (true = detected)

#### `status`

Current operational status

## Optional Properties

| **Property** | **Data Type** | **Range/Values** | **Unit** | **Permissions** |
|--------------|---------------|------------------|----------|-----------------|
| `concentration` | Uint | 0–65535 | ppm | RO (Read-Only) |
| `active` | Bool | - | - | RO (Read-Only) |
| `fault` | Uchar | 0–255 | - | RO (Read-Only) |
| `tampered` | Bool | - | - | RO (Read-Only) |

---

### Property Details

#### `concentration`

Measured concentration level

#### `active`

Indicates if the channel/sensor is currently active and functioning

#### `fault`

Fault code or diagnostic indicator (0 = no fault)

#### `tampered`

Indicates if the device has been tampered with

